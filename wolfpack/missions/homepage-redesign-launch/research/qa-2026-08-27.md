# Launch QA record — 2026-08-27 (pre-deploy)

## Automated
- node --test (archive + build + lead): 43/43 pass.
- check:links: 2,414 internal refs across 28 HTML files all resolve in dist/.
- Playwright responsive suite: 61/61 pass.
  - 7 routes (/, /services/, /hydro-jetting/, /property-managers/, /general-contractors/, /scottsdale/, /contact/) x 8 widths (320, 360, 390, 430, 768, 1024, 1280, 1440): zero horizontal overflow (<=1px), zero console/page errors.
  - Mobile nav + drawer keyboard reachable; Escape closes both.
  - Drawer wizard: continue gated on need selection; success panel ONLY on 200 from /api/lead; 502 shows honest error with call/email fallback.
  - Theme toggle persists via localStorage wp-v2-theme across reload.
  - Reduced motion: loader removed, marquee animation none.
  - All header/mobile-nav tap targets >= 44px; every image has an alt attribute.
- Banned-image scan (old AI set): zero references in dist.

## Fixed during QA
- Header theme/menu buttons 38px -> 44px (tap target floor).
- /contact/ 16px overflow at 320px (cpage-way-copy min-width:0 + overflow-wrap).
- Air-compressor "On the job" figure crop clipped subject (agent fix, re-verified).
- GC grid 260px min produced orphan cells at 1440 -> 300px min.

## Visual passes (human-graded screenshots at 1440 + 390)
Homepage, hydro-jetting, services, air-compressor, emergency, water-heaters,
property-managers, general-contractors, scottsdale, contact. Comp-fidelity ruling and
named doubts: wolfpack-site/wolfpack-site.decision.md.

## Outstanding at time of record
- RESEND_API_KEY not yet on the wolfpack-companies Vercel project (blocked for the agent;
  Patrik to paste). Form fails honest until set.
- Live-form end-to-end send to both inboxes: pending key; to be verified on preview.
