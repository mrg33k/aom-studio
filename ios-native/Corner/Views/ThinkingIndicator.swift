// Intentionally empty. The "see the thinking" animation (ThinkingArc,
// ShimmerModifier, .shimmering) lives in ChatView.swift so it lands in the
// target's compile list without a project.pbxproj edit. This file is not in the
// Xcode target; kept as a stub to avoid a duplicate-symbol build if it ever is.
