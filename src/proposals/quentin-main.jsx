import React from 'react'
import ReactDOM from 'react-dom/client'
import QuentinProposal from './QuentinProposal.jsx'
import '../index.css'

ReactDOM.createRoot(document.getElementById('proposal-root')).render(
  <React.StrictMode>
    <QuentinProposal />
  </React.StrictMode>,
)
