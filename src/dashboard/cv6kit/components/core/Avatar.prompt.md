Round identity avatar. Defaults to the brand gradient (`--avatar`) with an initial; pass `src` for a photo. On mobile the avatar doubles as the menu button — use `ring` there.

```jsx
<Avatar initial="P" size={40} />
<Avatar src="/me.jpg" size={32} />
<Avatar initial="P" ring />   {/* menu button */}
```
