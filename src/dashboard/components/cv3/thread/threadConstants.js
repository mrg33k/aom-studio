// Slugs that have a tmux super-agent session managed by relay-keepalive.py
// and can be hard-reset via the Control tab in chat settings.
export const RESETTABLE_AGENTS = new Set(['elon', 'studio'])
