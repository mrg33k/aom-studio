import React from 'react'
import ReactDOM from 'react-dom/client'
import SystemPage from './SystemPage.jsx'
import '../index.css'

ReactDOM.createRoot(document.getElementById('system-root')).render(
  <React.StrictMode>
    <SystemPage />
  </React.StrictMode>,
)
