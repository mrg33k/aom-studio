# corner:corner-smooth-loop — Research

Inputs: `corner-convex/FRONTEND-AUDIT.md`, `corner-convex/NATIVE-IOS-AUDIT.md` (both 2026-09-05, static audits from a Linux session), `corner-convex/e2e/visual.spec.ts` (layout contract). Prior loop precedent: `AOM-EA/scripts/muse-loop.sh` and `AOM-EA/scripts/grade-ios-simulator.sh` (proven simulator xcodebuild flags).
