# Corner feature inventory

Filled by the round-0 inventory agent. One row per user-facing feature. Both platforms in one table.

Format: `| feature | entry path (web) | entry path (native) | expected behaviour | web test | native tour step | last screenshot (web) | last screenshot (native) |`
