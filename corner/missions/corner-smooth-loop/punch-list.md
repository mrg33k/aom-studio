# Punch list

Format: `| id | platform | screen | what is wrong | why it fails the bar | file:line | proposed fix | status |`
Status: open / fixing / fixed (round) / wontfix (reason) / needs-patrik (reason)

| id | platform | screen | what is wrong | why it fails the bar | file:line | proposed fix | status |
|---|---|---|---|---|---|---|---|
| P001 | web | room (any) | The "no unread badge" test checks a class (`.unread-badge`) the code no longer renders (`.rrow-dot`), so it passes even if badges regress. | a silent test is worse than no test | corner-convex/e2e/visual.spec.ts (unread assertion); corner-convex/src/routes/Home.tsx:75 | assert on `.rrow-dot` (count and visibility) and add a fixture room with unread so the positive case is photographed | fixed (R1); evidence: /Users/aom-inhouse/aom-studio-transfer/corner-convex/e2e/__screenshots__/iphone-15-pro/home-unread.png |
| P002 | web | room / tool follow-up | Tool follow-up sends `userId: null`, the backend rejects it, and the error is swallowed, so the user sees nothing happen. | "clicked and nothing happened" is the definition of a failure | corner-convex/src/routes/Chat.tsx:143 | pass the signed-in user id; surface send failures inline like a normal send error | fixed (R1); evidence: /Users/aom-inhouse/aom-studio-transfer/corner-convex/e2e/__screenshots__/iphone-15-pro/room-send-failed.png |
| P003 | web | room (desktop) | Short threads sit at the top, leaving a roughly 400 px empty gap between the last message and the composer. | the emptiest screen in the suite; reads unfinished at 1440 px | corner-convex/src/routes/Chat.tsx (thread container), src/polish.css | recommendation: keep top anchoring (Slack/ChatGPT do) but let the empty welcome state fill the column with the room's agent, purpose, and three suggested prompts; Patrik decides | needs-patrik (design direction) |
| P004 | web | room (entry) | `.main > * { animation: none }` in polish.css is dead because index.css wins by import order; the 0.2 s enter fade still runs. | a rule that looks like it turns motion off but does not will mislead the next edit | corner-convex/src/polish.css:32, src/index.css:372 | delete the dead rule or make the intent explicit (keep the fade, remove the override) | fixed (R1); evidence: /Users/aom-inhouse/aom-studio-transfer/corner-convex/e2e/__screenshots__/iphone-15-pro/room.png |
| P005! | native | 00-signin | The sign-in backdrop redraws a full-screen ASCII canvas every frame; the app sits at ~57% CPU on the sign-in screen and the main thread never idles (UI tests cannot even snapshot it). | battery drain and a laggy first screen on a cold launch; the first thing a new user feels | ios-native/Corner/Views/ASCIIBackground.swift:37 (TimelineView(.animation) + Canvas), SignInView.swift:310 (repeatForever pulse) | cap the canvas at ~12 fps with `TimelineView(.periodic(from:by:))`, cache glyph runs, pause when Reduce Motion is on; static frame under `-screenTour` | open |
| P006 | native | 02-home | The "Waiting on you" review row (`waitingRow`) is defined but never inserted into the home list, so the review queue has no entry point from home. | a feature that exists but cannot be reached | ios-native/Corner/Views/RoomListView.swift:663 | wire `waitingRow` into the Tools section when the review queue is non-empty; the tour's `16-review` frame proves it | open |
| P007 | native | 20-background-work | The Background Work sheet has no Done/Close button; drag-to-dismiss only. | a sheet with no visible way out feels broken | ios-native/Corner/Views/BackgroundWorkView.swift | add a toolbar Done button like the other sheets | open |
| P008! | web | email (phones) | The tab row (Needs you / Watching / All / Refresh / Connect Gmail) does not wrap or scroll, so Refresh is clipped and Connect Gmail is unreachable on 320-390 px. | core actions unreachable on a phone | corner-convex/src/routes/Email.tsx (tab row) | make the chip row horizontally scrollable with edge fade, or wrap; remove the layoutContract carve-out once fixed | open |
| P009 | web | files (phones) | Room-picker chips carry full titles with no truncation; the Wolfpack chip overflows the viewport. | content hanging off the edge | corner-convex/src/routes/Files.tsx (room chips) | truncate chip labels with ellipsis at ~14ch and let the row scroll; remove the carve-out | open |
| P010 | web | files / tracker / notifications (light) | Page headings (`h1.t-24`) set no colour and wash out on the light theme. | unreadable title on one theme | corner-convex/src/routes/Files.tsx, Tracker.tsx, Notifications.tsx; token in src/index.css | give `.t-24` the ink token so all three headings follow the theme | open |
| P011 | web | settings | No sign-out on the Settings page (only in the drawer) and no usage readout. | the one place people look for sign-out does not have it | corner-convex/src/routes/Settings.tsx | add a Sign out row at the bottom in the existing row style; usage can wait | open |
| P012 | web | composer / upload | The paperclip uploads straight into room files with no chip, preview, or confirmation in the composer; the sent message carries nothing. | "clicked and nothing visible happened" | corner-convex/src/components/Composer.tsx, src/routes/Chat.tsx:158 | minimal: after upload show a small "Added to room files: <name>" chip above the composer for a few seconds with a link to the files sheet; attachment staging is a later feature | open |
| P013! | web | room / tool follow-up | The tool follow-up now posts the summary as a user bubble, so the agent echoes its own summary back. | a conversation that repeats itself feels broken | corner-convex/src/routes/Chat.tsx:154 | send follow-ups via `messages:agentReply` with the room's agent slug (or whatever the backend exposes) so the summary lands as the agent's message; keep the failure UI | open |
| P014 | web | sign-in (sent state) | The "Check your email" card is pre-empted by the session redirect when the backend answers fast, so a person may never see the send confirmation. | no feedback after the one action on the page | corner-convex/src/routes/Auth.tsx (existing-session effect) | hold the card for ~900 ms before redirecting when a link was just sent; verify on the real backend | open |
| P015! | web | notifications-dark/light/glass, all sizes | Rows show raw internal type strings ("Design mentioned you · mention", "Research finished · turn_complete", "New file · file") and full-second timestamps ("9/5/2026, 11:22:55 AM") | internal event names and machine timestamps are debug text, not a product surface | corner-convex/src/routes/Notifications.tsx:27 (`· {n.type}`), :30 (`toLocaleString()`) | Render human labels (Mention / Turn finished / File) and short friendly times ("9:33 AM", "Yesterday"). | open |
| P016! | web | settings-light / desktop + iphone-se | Light pill is selected but the page renders fully dark (identical to settings-dark); every other `*-light` frame in the suite renders light | selecting Light on the one page that owns the theme switch visibly does nothing | corner-convex/src/routes/Settings.tsx:22-29 (writes `html[data-theme]` only); corner-convex/src/App.tsx:112 (tokens live on `.shell[data-theme]`) | Use one theme source consumed by both App and Settings, with tokens on `:root[data-theme]`. | open |
| P017 | web | tracker-dark/glass, desktop + iphone | Subtitle reads "Issues and tickets per room. Per-room filter will use Convex `tracker` table next." — roadmap copy with backticks, user-visible | developer-facing notes in the product read unfinished | corner-convex/src/routes/Tracker.tsx:23 | Replace with a user-facing line ("Issues and tickets per room.") and move the Convex note to a code comment. | open |
| P018 | web | tracker-light / desktop + iphone | Issue titles wash out on the white cards and the "open" pill is near-black (`#262629`) with dark-grey text, illegible | unreadable content on one theme | corner-convex/src/routes/Tracker.tsx:29-31 (hardcoded `#9a9aa0`, `#262629` literals) | Tokenize title, subtitle, and pill colors so all three statuses adapt to the theme. | open |
| P019 | web | email-compose / desktop + both phones | Footer reads "via Arcade Gmail.SendEmail" (desktop clips it to "via Arca…"; phones wrap it over two lines) — a raw API method name in the product | internal integration identifiers leak into user-visible copy | corner-convex/src/routes/Email.tsx:201 | Show a user-facing string such as "via Arcade · Gmail". | open |
| P020 | web | email-compose / desktop + iphone-se | Desktop shows no Cc control at all; on iphone-se a "Cc" button is squeezed half-clipped at the To row's right edge | the Cc feature from features.md F065 is missing on desktop and broken on small phones | corner-convex/src/routes/Email.tsx:153 (`showCc` toggle button with no flex guard) | Give the Cc toggle `flex:none` and keep it visible on all widths; add the Cc row by default on ≥600px. | open |
| P021! | web | room-tool / desktop + iphone-15-pro + iphone-se | The tool follow-up renders garbled: agent bubble starts `Got it — "'· Found 3 emails from Kraken Corps` (stray quote + bullet colliding); desktop additionally shows the tool result as a second blue "You" bubble plus two near-identical "Got it" echoes | a conversation that stutters and quotes itself feels broken | corner-convex/src/routes/Chat.tsx:167-178 (summary starts with `•`, then passes through the generic auto-reply echo that wraps text in `Got it — "…"`) | Keep agent-authored tool summaries out of the generic auto-reply echo path so the summary lands once, verbatim, as the agent. | open |
| P022 | web | room-send-failed / desktop + iphone-se | The older agent "Summary so far" bubble renders faded/washed while the failed blue bubble stays vivid — the `opacity:.7` for `temp-*` ids (Chat.tsx:313) also catches failed agent follow-ups (`temp-fail-*`) | a confirmed-visible message looking half-rendered reads as a rendering glitch | corner-convex/src/routes/Chat.tsx:313 (`startsWith("temp-") ? 0.7 : 1`) | Dim only unconfirmed optimistic sends (`temp-`, excluding `temp-fail-`); render failed messages at full opacity with the retry affordance. | open |
| P023! | web | home-agents / desktop + both phones | The agent grid's last row slides half-under the sticky composer (phones lose ~half a card row; "Operations / Ships on time" is unreadable without overscroll) | content the composer covers cannot be reached by scrolling | corner-convex/src/polish.css:38 (`.home-scroll` bottom padding 24px, far less than composer height) | Raise `.home-scroll` bottom padding to clear the composer (≈110px). | open |
| P024! | web | home + home-projects + home-agents / iphone-se | The filter chip row half-clips "Missions" ("M…") at 320px with no visible scroll affordance (it fits in the loading state only because counts are hidden) | a filter tab the user cannot see exists cannot be tapped | corner-convex/src/routes/Home.tsx:191 (chips-row), corner-convex/src/polish.css:42-43 (hidden scrollbar, subtle edge fade) | Hide chip counts below 360px or add scroll snap plus a stronger edge fade. | open |
| P025 | web | home-palette / iphone-se | The last palette result (Wolfpack, 3 lines) is cut mid-text at the viewport bottom with no scroll affordance; it fits on 15-pro | a search result the user cannot read might as well not match | corner-convex/src/components/CommandPalette.tsx:95-103 (fixed `top:12vh` panel, no viewport-capped scroll region) | Cap the palette list height to the viewport (`dvh`) with its own internal scroll. | open |
| P026! | web | auth-light / desktop + iphone-15-pro | The ◆ logo mark renders near-white on the light card, nearly invisible | the brand mark disappearing on one theme looks broken on the first screen | corner-convex/src/routes/Auth.tsx:105,127 (`◆` with `var(--fg)`/unstyled color that stays light) | Give the mark an ink-token color so it stays visible on light and glass. | open |
| P027 | web | email-detail / desktop | The "Back to inbox" bar is a full-bleed near-black band visibly darker than the page ground around it | one mismatched band makes the header look patched on | corner-convex/src/routes/Email.tsx:232 (back-row bar) | Ground the back bar with the same surface token as the page instead of near-black. | open |
| P028 | web | home-palette / desktop | The open palette's top edge bisects the "ROOMS" eyebrow label behind it (label half-visible above the panel) | a section header peeking over a modal edge looks sloppy next to the phone frames, where it is fully covered | corner-convex/src/components/CommandPalette.tsx:95-103 (panel position vs content) | Start the panel below the section header or dim the background content while open. | open |
| P029 | web | home-composer-typed / iphone-se | The typed text's first glyph ("Friday launch checklist") is clipped at the field's left edge; it reads whole on 15-pro and desktop | typed text the field itself cuts off looks like a rendering bug while typing | corner-convex/src/routes/Home.tsx:257-266 (home-composer input) | Add adequate left padding to the home composer input and reset `scrollLeft` on change. | open |
| P030! | web | notifications-dark / all sizes | Row titles ("Design mentioned you", "Research finished") render in a near-ground grey and are almost unreadable; only the second line is legible. | the one line that says what happened is the one you cannot read | corner-convex/src/routes/Notifications.tsx:27 (title span colour) | use the ink token for titles, muted token for the second line; unread rows get the strong weight | open |
| P031! | native | 00-signin / 01c-signin-rejected, all devices | The ASCII glyph field runs straight through the form: labels, the email field, and the red error strip all sit on live glyphs, and the field is densest exactly behind the form. | the first screen looks busy where it should be calm; the error strip is readable only because it is red | ios-native/Corner/Views/SignInView.swift (form container), ASCIIBackground.swift (centre scrim / gather bias) | put a soft opaque scrim behind the form column (blur or 80% ground) so labels and the error strip sit on a clean surface; keep the field visible above and below | open |
